org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental-compilation cache has been corrupting mid-build on this machine
# ("Could not close incremental caches") — almost certainly Windows Defender scanning the
# cache files while Kotlin is still writing them. Disabling incremental compilation trades
# some rebuild speed for reliability.
kotlin.incremental=false
